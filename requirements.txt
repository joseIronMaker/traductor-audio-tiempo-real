# === Núcleo (necesario desde la Fase 1) ===
google-genai>=1.0.0      # Cliente oficial de Gemini (Live API)
python-dotenv>=1.0.0     # Carga GEMINI_API_KEY desde .env
numpy>=1.24              # Manejo de buffers de audio
scipy>=1.11              # Resampleo 48k/44.1k -> 16k
soundfile>=0.12          # Leer/escribir WAV/FLAC (trae libsndfile incluido en Windows)

# === Captura y reproducción del sistema (Fase 2 en adelante) ===
sounddevice>=0.4.6       # Reproducir/grabar eligiendo dispositivo
pyaudiowpatch>=0.2.12    # Captura WASAPI loopback (lo que suena en Windows)
